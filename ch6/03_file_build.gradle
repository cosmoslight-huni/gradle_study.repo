//환경 구성 정의
configurations {
	conf1
}


//의존관계 정의
dependencies {
	// 1. 파일 의존 관게 - 특정 파일이나 라이브러리 지정
	conf1(files('lib/commonLib.jar'))
	// 2. 파일 의존 관계 - 디렉토리 지정
	conf1(fileTree(dir:'lib', include:'**/*.jar'))
}


task exeTask << {
	configurations.conf1.each{
		println it.absolutePath
	}
}