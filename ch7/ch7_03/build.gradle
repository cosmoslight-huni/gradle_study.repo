task exeTask << {
	//Groovy 문법 사용 - 환경설정 정보 참조
	def envBlock = new File("EnvDir/conf.gradle").toURL()
	//개발 환경
	println "****************Dev  Environments********************" 
	def dev = new ConfigSlurper("dev").parse(envBlock)
	println "Dev Server IP    : ${dev.server_ip}" 
	println "Dev Domain Name  : ${dev.domain_nm}"

	//검증 환경
 	println "****************Mid  Environments********************" 
	def mid = new ConfigSlurper("mid").parse(envBlock)
	println "Mid Server IP    : ${mid.server_ip}" 
	println "Mid Domain Name  : ${mid.domain_nm}"

	//운영 환경
 	println "****************Prod Environments********************" 
	def prod = new ConfigSlurper("prod").parse(envBlock)
	println "Prod Server IP   : ${prod.server_ip}" 
	println "Prod Domain Name : ${prod.domain_nm}"
}